"use client";

import Link from "next/link";

export default function DashboardPage() {
  return (
    <main className="p-6 space-y-6">
      <header>
        <h1 className="text-3xl font-semibold">Dashboard</h1>
        <p className="text-sm text-white/60">
          This page shows your current state. Nothing runs automatically.
        </p>
      </header>

      <section className="glass-card p-6 space-y-2">
        <h2 className="text-lg font-semibold">Analyzer</h2>
        <p className="text-sm text-white/60">
          Run the Analyzer only when you are ready. Each run is logged.
        </p>
        <Link href="/dashboard/analyzer" className="btn glow-neon">
          Go to Analyzer
        </Link>
      </section>

      <section className="glass-card p-6 space-y-2">
        <h2 className="text-lg font-semibold">Vault</h2>
        <p className="text-sm text-white/60">
          Read-only records generated by your explicit actions.
        </p>
        <Link href="/dashboard/documents" className="btn glow-soft">
          Open Vault
        </Link>
      </section>
    </main>
  );
}