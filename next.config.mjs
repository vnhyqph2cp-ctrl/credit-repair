/** @type {import('next').NextConfig} */
const nextConfig = {
  turbo: {
    enabled: false,
  },
  // Optional: if Next complains about experimental flags, you can remove this comment and adjust here later
};

export default nextConfig;
